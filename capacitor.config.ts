import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.barcode.scanner',
  appName: 'Barcode_Scanner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
